<%= "chore(backport): #{ENV.fetch('GIT_PR_RELEASE_BRANCH_STAGING')} to #{ENV.fetch('GIT_PR_RELEASE_BRANCH_PRODUCTION')}" %>

> [!NOTE]
> この Pull Request は .github/workflows/git-pr-backport.yml によって自動的に作成されています。

作成条件と生成されるPull Requestは以下のとおりです。

| 作成条件 | Pull Request |
| --- | --- |
| release to master のマージ | master to release |
| master to release のマージ | release to patch |
| release to patch のマージ | patch to develop |
